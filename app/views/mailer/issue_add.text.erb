<%= l(:text_issue_added, :id => "##{@issue.id}", :author => @issue.author) %>

----------------------------------------
<%= render :partial => 'issue', :formats => [:text], :locals => { :issue => @issue, :issue_url => @issue_url } %>
<%= Setting.plugin_redmine_email_footers['issue_added'] %>
